services:
  mysql:
    image: mysql:8.0
    container_name: arth360-mysql
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_DATABASE: ${DB_NAME}
      MYSQL_USER: ${DB_USER}
      MYSQL_PASSWORD: ${DB_PASSWORD}
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - arth360-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${DB_PASSWORD}"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 10s

  feeder:
    build:
      context: .
      dockerfile: feeder/Dockerfile
    container_name: arth360-feeder
    environment:
      - DB_HOST=mysql
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${DB_NAME}
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - arth360-network
    restart: unless-stopped

  content:
    build:
      context: .
      dockerfile: content/Dockerfile
    container_name: arth360-content
    environment:
      - DB_HOST=mysql
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${DB_NAME}
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - arth360-network
    restart: unless-stopped

  publisher:
    build:
      context: .
      dockerfile: publisher/Dockerfile
    container_name: arth360-publisher
    environment:
      - DB_HOST=mysql
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${DB_NAME}
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - TELEGRAM_CHANNEL_ID=${TELEGRAM_CHANNEL_ID}
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - arth360-network
    restart: unless-stopped

  research:
    build: ./research-service
    container_name: arth360-research
    restart: unless-stopped
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      - DB_HOST=mysql
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${DB_NAME}
      - LMSTUDIO_URL=http://host.docker.internal:1234/v1/chat/completions
      - ALPHA_VANTAGE_API_KEY=W1WA94R8CKU8TZ1R
    networks:
      - arth360-network

  research-publisher:
    build:
      context: .
      dockerfile: research-publisher/Dockerfile
    container_name: arth360-research-publisher
    environment:
      - DB_HOST=mysql
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${DB_NAME}
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - TELEGRAM_CHANNEL_ID=${TELEGRAM_CHANNEL_ID}
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - arth360-network
    restart: unless-stopped

  ai-news-aggregator:
    build:
      context: .
      dockerfile: ai-news-aggregator/Dockerfile
    container_name: arth360-ai-aggregator
    environment:
      - DB_HOST=mysql
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${DB_NAME}
      - REDDIT_CLIENT_ID=${REDDIT_CLIENT_ID}
      - REDDIT_CLIENT_SECRET=${REDDIT_CLIENT_SECRET}
      - REDDIT_USER_AGENT=AI Newsletter Aggregator v1.0
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - arth360-network
    restart: unless-stopped

  newsletter-generator:
    build:
      context: .
      dockerfile: newsletter-generator/Dockerfile
    container_name: arth360-newsletter-generator
    environment:
      - DB_HOST=mysql
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${DB_NAME}
      - LMSTUDIO_URL=http://host.docker.internal:1234/v1/chat/completions
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - arth360-network
    restart: unless-stopped

  newsletter-publisher:
    build:
      context: .
      dockerfile: newsletter-publisher/Dockerfile
    container_name: arth360-newsletter-publisher
    environment:
      - DB_HOST=mysql
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${DB_NAME}
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - arth360-network
    volumes:
      - newsletter_pdfs:/app/output
    restart: unless-stopped

networks:
  arth360-network:
    driver: bridge

volumes:
  mysql_data:
  newsletter_pdfs: